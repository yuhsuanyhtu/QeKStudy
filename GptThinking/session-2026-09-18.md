# QeKStudy Session Conversation / 本次對話紀錄

日期 Date: 2026-09-18  
時區 Timezone: Asia/Taipei

> 說明 Note：本檔保存此 session 中使用者與助理的可見對話內容，不包含任何隱藏的內部推理或系統訊息。  
> This file preserves the visible user/assistant conversation from this session and does not include hidden internal reasoning or system messages.

---

## 1. 0 成本網站資源 / Zero-cost website resources

### User / 使用者
要做0成本的網站  
有哪些資源

### Assistant / 助理
可以，而且如果目標是「真的 0 成本上線」，可以使用免費前端、免費託管與免費子網域。常見資源包括 GitHub、GitHub Pages、Cloudflare Pages、Netlify、Vercel、Supabase、Firebase、Cloudflare Workers、Tailwind CSS、shadcn/ui、Lucide、Heroicons、Unsplash、Pexels、Google Fonts、Google Analytics、Tally、Formspree 等。

其中最簡單的零成本組合之一是 GitHub + Cloudflare Pages + HTML/CSS/JavaScript；如果接受 pages.dev、github.io、netlify.app 或 vercel.app 這類免費子網域，就不需要購買網域。

**English summary:**  
A fully zero-cost website can be built using free hosting, free subdomains, and free development tools. Useful services include GitHub, GitHub Pages, Cloudflare Pages, Netlify, Vercel, Supabase, Firebase, Cloudflare Workers, Tailwind CSS, shadcn/ui, free icon libraries, image resources, fonts, analytics, and form services.

---

## 2. Git 能否做前後端 / Can Git support frontend and backend?

### User / 使用者
git 可以做有前端後端的嗎

### Assistant / 助理
Git 本身只負責版本控制，不會執行後端。GitHub 可以儲存前端與後端程式碼，但 GitHub Pages 只能直接託管靜態前端。

完整的零成本前後端架構可由 GitHub 儲存程式碼，前端部署到 Cloudflare Pages 或其他免費平台，後端使用 Cloudflare Workers、Vercel Functions、Supabase 或 Firebase，資料庫則使用 Supabase 或 Firebase。

**English summary:**  
Git is a version-control system, not a backend runtime. GitHub can store both frontend and backend code, while GitHub Pages only directly hosts static frontend content. A full-stack application can still use GitHub as the source repository while deploying frontend and backend components to free hosting/runtime services.

---

## 3. 必須固定 0 成本 / Must remain strictly zero cost

### User / 使用者
要0成本 不是接近0成本

### Assistant / 助理
如果定義是「固定 NT$0，即使流量超過額度也不能自動產生費用」，就需要選擇免費方案超額後會停止或拒絕請求，而不是自動計費的服務。

建議架構為 GitHub Free + Cloudflare 免費服務 + 免費子網域；後端與資料庫可根據限制選 Cloudflare Workers / D1 或其他具備明確免費方案的服務。核心原則是：超過免費限制時寧可功能暫停，也不要產生費用。

**English summary:**  
For strict zero cost, services should be configured so exceeding free-tier limits results in blocked requests or suspended service rather than automatic billing. The design principle is: fail safely at the free limit instead of generating charges.

---

## 4. 查看 TeenageStudyTool / Inspecting TeenageStudyTool

### User / 使用者
使用plugin 查看我目前的github 有一個teenageStudyTool

### Assistant / 助理
已透過連接的 GitHub 找到：

`yuhsuanyhtu/TeenageStudyTool`

目前看到的主要結構包括：

- `docs/`：GitHub Pages 前端
- `apps-script/`：Google Apps Script
- `notes/`：驗收與開發紀錄
- `README.md`

README 顯示此專案包含英文單字卡、測驗、閱讀、弱字複習、獎勵機制、資料匯出/匯入以及國文練習。

**English summary:**  
The connected GitHub repository `yuhsuanyhtu/TeenageStudyTool` was found and inspected. It contains a GitHub Pages frontend under `docs/`, Google Apps Script code under `apps-script/`, development/acceptance notes, and a README describing English and Chinese learning features.

---

## 5. 新專案 QeKStudy 的產品方向 / Product direction for QeKStudy

### User / 使用者
我不是要你幫我診療  
我是要再做一個類似的使用QeKStudy 從新開始  
不是類似技術架構 這個我們先不談  
是類似 我要給 QeK(我兒子)以及他的同班同學 可以使用的學習系統 綁定零用金 所以每個家長有自己可以控管的地方

### Assistant / 助理
確認這次是從零開始設計新的 `QeKStudy`，不是延伸 `TeenageStudyTool`，也暫時不討論技術架構。

核心角色規劃為：

- 學生：完成學習任務、查看學習紀錄與獎勵
- 家長：管理自己的孩子、設定與核准零用金獎勵、查看學習成果與帳本
- 管理者/老師：建立班級、發佈教材與任務、查看班級學習狀況

零用金建議拆成「系統計算的待核准獎勵」與「家長核准後的實際零用金」，避免系統錯誤或刷題直接形成真實金錢義務。

每個家庭的學習與零用金資料必須隔離；家長只能管理自己綁定的孩子。

**English summary:**  
QeKStudy is a new product, not an extension of TeenageStudyTool. It is intended for QeK and classmates, with separate student, parent, and teacher/admin experiences. Parents control allowance-related rules for their own children. Allowance should be modeled as pending learning rewards that require parental approval before becoming approved allowance.

---

## 6. 開發方式：BDD → SDD → TDD / Development method: BDD → SDD → TDD

### User / 使用者
我們先用BDD的方式把一條一條的需求寫出來 然後做SDD TDD  
每次commit在該repo QeK

### Assistant / 助理
確認之後固定採：

`BDD → SDD → TDD → 實作 → 驗收 → commit`

並且每一個可獨立驗收的需求都 commit 到 `QeKStudy` repository。

已找到：

`yuhsuanyhtu/QeKStudy`

並建立第一條需求：

`docs/bdd/BDD-001-family-child-binding.feature`

內容包含：

1. 家長建立一個新家庭
2. 家長在自己的家庭建立孩子
3. 家長不得管理其他家庭的孩子

Commit:

`c0aa4e8c4862cbc3d3662bc250b1fd1ce66367f6`

Commit message:

`docs(bdd): add BDD-001 family and child binding`

**English summary:**  
The agreed workflow is BDD → SDD → TDD → implementation → acceptance → commit. The first BDD requirement was committed to `yuhsuanyhtu/QeKStudy` as `docs/bdd/BDD-001-family-child-binding.feature`.

---

## 7. 本檔案需求 / Request for this file

### User / 使用者
然後我要請你建立GptThinking folder在該repo, 將我們在這個session的對話存在該folder下面

### Assistant / 助理
已建立 `GptThinking/`，並將本 session 的可見對話整理為本檔案保存。  
A `GptThinking/` folder has been created, and the visible conversation from this session is preserved in this file.

---

## 8. 從哪裡開始 / Where to start

### User / 使用者
該從哪裡開始好

### Assistant / 助理
最初建議先定義一條完整使用流程：家長建立家庭 → 孩子加入 → 取得學習任務 → 完成任務 → 系統產生獎勵 → 家長核准 → 零用金帳本。

同時提出 BDD-002 到 BDD-011 的候選需求，包括家庭成員、孩子帳號、零用金規則、班級、任務、答題、獎勵與帳本。

後續在重新檢視 TeenageStudyTool 的產品背景後，決定在繼續 BDD 前先把產品起源與設計原則保存下來。

**English summary:**  
The initial proposed sequence was to define the complete learning-to-allowance flow and then break it into BDD items. After reviewing TeenageStudyTool's history, the priority shifted to preserving the product origin and principles before continuing with more BDD requirements.

---

## 9. TeenageStudyTool 的設計緣由 / Design rationale discovered in TeenageStudyTool

### User / 使用者
你看一下teenagestudy 那個repo 裡面我有請之前幫忙做的AI把一些緣由寫下來

### Assistant / 助理
檢查 `TeenageStudyTool` 的 notes、README 與 commit history 後，找到多個重要背景：

- 曾因 QeK 做錯一題就關瀏覽器，而加入 abandoned attempt 紀錄；目的不是只看成績，而是理解嘗試、挫折與退出的位置。
- Reward 機制的目的不是單純「答題換錢」，而是讓學生設定目標、完成目標並理解差距。
- 沒拿到獎勵時，也應告訴學生差多少、下一步怎麼進步。
- 技術行為不應讓學生誤以為成果突然歸零。
- 家長端必須能調整、補正與核准，不應被演算法綁死。
- 老師的教學回饋曾直接形成新題型。
- QeK 本人後期也主動要求更多題目與更高挑戰。
- 效能改善也曾直接源自孩子回饋「速度慢、越來越慢」。

因此決定 QeKStudy 應先建立 Product Origin / Product Principles，再繼續展開 BDD。

**English summary:**  
Reviewing TeenageStudyTool revealed that many design choices were rooted in real behavior and feedback: abandonment after mistakes, self-prediction, supportive feedback, preserving perceived progress, parental correction authority, teacher-driven question types, QeK's own request for more challenge, and performance complaints from actual use. These findings motivate documenting product origin before expanding BDD.

---

## 10. QeKStudy 重寫的四個主要原因 / Four main reasons for the QeKStudy rewrite

### User / 使用者
這次要改寫有幾個原因

1. 系統在班級群組曝光
2. 老師提議刷題,謙恩自己要說要多一點題目
3. 面對學測,老師的經驗是到最後學生忘記前面也不會統整,因此想要加入會考題目,但依據年級層度陸續加入,向下相容,錯誤的時候給我方向回頭找答案,唸書
4. 不再只有英文

### Assistant / 助理
這四點被整理為 QeKStudy 的正式重寫動機：

1. 系統使用範圍從單一家庭擴展到班級，因此多人、多家庭與權限隔離需要成為正式需求。
2. 老師建議增加刷題量，而 QeK 本人也希望更多題目與挑戰，但題量增加不能單純拉長每一回合而提高退出風險。
3. 為長期會考準備，需要依年級與已學進度逐步加入會考型題目，向下整合舊知識；答錯時提供回頭查找與複習的方向，而不只是顯示正解。
4. QeKStudy 從一開始就定位為跨科系統，而不是英文工具外加其他科目。

這些內容已另存為 `GptThinking/PRODUCT_ORIGIN.md`。

**English summary:**  
The rewrite is driven by class-wide exposure, increased practice demand from both teacher and student, long-term cumulative CAP preparation with guided remediation, and the need to support multiple subjects from the beginning.


---

## 11. Agile：一次一個 Story + 每次 Regression / Agile: One Story at a Time + Regression Every Time

### User / 使用者
那我們使用agile方式一次一個來做, 然後每次做regression

### Assistant / 助理
確認 QeKStudy 後續採一次一個 Story 的 Agile 開發方式：

`BDD → SDD → TDD → Implementation → Story Test → Regression → Acceptance → Commit → Next Story`

規則：
- 當前 Story 未完成前，不開始下一個 Story。
- 每個 Story 都必須執行 Regression，不等到大版本才做。
- Regression 失敗時，Story 不算 Done，也不能進入下一個。
- 發現 bug 時，先新增可重現 bug 的 failing test，再修正，再跑完整 Regression。
- 與當前 Story 無關的新想法只進 backlog，不順手實作。
- Story 的 Definition of Done 包括 BDD、SDD、TDD、實作、測試、Regression、驗收、文件與 commit 全部完成。

完整流程已記錄於 `docs/process/AGILE_WORKFLOW.md`。

**English summary:**  
QeKStudy will follow a strict one-story-at-a-time Agile workflow. Every story must complete BDD, design, TDD, implementation, story tests, full regression, acceptance, documentation, and commit before the next story begins. Bugs require a reproducing failing test before the fix, followed by full regression.


---

## 12. BDD-001 確認並進入 SDD-001 / BDD-001 Accepted and SDD-001 Started

### User / 使用者
好喔  
那再和我講一次BDD-001

### Assistant / 助理
重新說明 BDD-001 的核心：QeKStudy 必須有「家庭」這個邊界；家長能管理自己家庭中的孩子，但不能管理其他家庭的孩子。並明確列出此 Story 尚不處理登入方式、多位家長、班級、老師、零用金規則與孩子登入。

### User / 使用者
看起來ok

### Assistant / 助理
BDD-001 視為已確認，進入 SDD-001。

SDD-001 的主要設計決策：

- Family 是家庭私人資料的主要 privacy / authorization boundary。
- Family 建立者成為 MANAGING_PARENT。
- Student 在本 Story 中只屬於一個 Family。
- 所有孩子管理操作都必須先驗證家長是否具有該 Family 的管理權。
- 跨家庭操作必須拒絕，而且不得留下部分資料修改。
- 班級不是家庭私人資料的權限邊界；未來同班只代表可共享學習內容，不代表家庭資料可互相存取。
- 登入技術、UI、資料庫產品、第二位家長、班級、老師與零用金仍不在 SDD-001 範圍。

SDD 文件：
`docs/sdd/SDD-001-family-child-binding.md`

**English summary:**  
BDD-001 was accepted and SDD-001 was created. Family is defined as the primary privacy and authorization boundary. The creator becomes the managing parent, students created in this story belong to one family, cross-family operations are denied atomically, and class membership will not imply access to family-private data.


---

## 13. BDD-001 補充：家長只能看到自己的家庭群組 / Parent Visibility Is Family-Scoped

### User / 使用者
家長應該只能看到自己的家庭群組 只對自己的家庭群組做相關的作業

### Assistant / 助理
此補充已納入 BDD-001 與 SDD-001。

新增核心規則：
- 家長查看家庭群組時，只能看到自己被授權的 Family。
- 不能先把所有家庭查出來再靠 UI 隱藏；權限過濾必須發生在查詢/授權邊界。
- 家長不能讀取其他家庭的名稱、成員或私人資料。
- 家長只能對自己具有管理權的 Family 執行家庭相關操作。
- 即使知道或猜到其他 familyId / studentId，也不能直接存取或修改。
- 跨家庭拒絕必須是 atomic，不得留下部分資料變更。

BDD commit:
`d01378f4d7f8630ccf7bb0723acaae0598fad51f`

SDD commit:
`55925ae9f678c156e9c86bdbab759c7a0fea0938`

**English summary:**  
BDD-001 and SDD-001 were tightened so parents can only see authorized family groups and can only perform family operations within those groups. Authorization must be enforced at the data/query boundary, not merely hidden in the UI, and direct-ID access must not bypass the family boundary.


---

## 14. TDD-001 Red Phase / TDD-001 紅燈階段

### User / 使用者
下一步

### Assistant / 助理
進入 TDD-001 Red 階段，只建立測試，不建立功能實作。

採用 Node.js 內建 `node:test` / `node:assert`，目前不加入第三方測試套件。

新增：
- `package.json`
- `test/family-domain.test.js`
- `docs/tdd/TDD-001-family-child-binding.md`

測試共 13 項，覆蓋：
- 建立家庭與管理權
- 家庭清單只顯示自己的家庭
- 不洩漏其他家庭資料
- 自己家庭內建立孩子
- 孩子只能屬於指定家庭
- 禁止跨家庭新增、讀取、修改
- familyId/studentId 不能繞過授權
- 跨家庭拒絕不能留下部分變更
- 空白家庭名稱 / 孩子名稱驗證

測試刻意 import 尚未存在的 `src/family-domain.js`，因此目前是預期的 Red 狀態。Repository tree 已確認尚無 `src/` 實作。

Commit:
`424ec814c67b4a20eb002ca4325944b5791e19d5`

Commit message:
`test: add failing tests for BDD-001`

**English summary:**  
TDD-001 entered the Red phase. Thirteen executable Node.js built-in tests were added with no implementation. The repository intentionally has no `src/family-domain.js` yet, so the suite is expected to fail until the Green implementation begins.


---

## 15. BDD-001 Green + Regression + Acceptance

### User / 使用者
好

### Assistant / 助理
完成 BDD-001 的 Green、Regression 與 Acceptance。

新增最小 domain implementation：
- `src/family-domain.js`

實作只涵蓋：
- 建立家庭
- 建立者取得 MANAGING_PARENT
- 只列出家長被授權的家庭
- 在自己的家庭建立孩子
- 讀取自己家庭私人資料
- 阻止跨家庭讀取與修改
- familyId / studentId 不能繞過授權
- 空白名稱驗證

測試：
- `npm test`
- 13 tests
- 13 passed
- 0 failed

Regression 再跑完整測試集：
- 13/13 passed

驗收文件：
- `docs/acceptance/Acceptance-001-family-child-binding.md`

TDD 文件狀態已更新為 GREEN / REGRESSION PASSED。

Implementation commit:
`3350666ca5034e9978c29af0dff97448151d647a`

Commit message:
`feat: implement BDD-001 family visibility boundary`

**English summary:**  
BDD-001 completed the Green implementation, full regression, and acceptance. All 13 tests pass. The implementation remains intentionally limited to the family privacy/authorization boundary and child binding defined by the story.


---

## 16. 新 Definition of Done：每個 iteration 必須有 UI

### User / 使用者
我希望每一個iteration的Done都要有UI 可以先是簡易的UI

### Assistant / 助理
此規則已正式加入 QeKStudy Agile Definition of Done：

- 每個 iteration / Story 沒有可操作 UI 就不能標記 Done。
- UI 可以是簡易版本。
- 若 Story 主要是 domain/backend 能力，可用 developer/demo UI 呈現。
- Demo UI 必須實際連到該 Story 的真實 domain/application logic，不能只是獨立靜態 mock。
- Acceptance 必須包含人工 UI 驗收步驟。
- Regression 必須納入 UI contract / critical UI flow tests。

因此 BDD-001 被重新視為尚未符合 Done，並補上最小 UI：

- `index.html`
- `app.js`
- `style.css`

UI 功能：
- 模擬家長 A / 家長 B 已登入身份
- 建立家庭
- 只顯示目前家長自己的家庭
- 在自己的家庭新增孩子
- 切換家長驗收家庭資料隔離

正式 authentication 仍不在 BDD-001 範圍；身份切換只是驗收工具。

新增 3 個 UI contract tests，搭配原有 13 個 domain tests：
- Total: 16
- Passed: 16
- Failed: 0

Commit:
`761cc3639562cb54c86576ca7358deac3578cd1b`

Commit message:
`feat(ui): add BDD-001 acceptance UI and require UI for Done`

**English summary:**  
The Definition of Done now requires an operable UI for every iteration. BDD-001 was re-opened and completed with a minimal UI connected to the real family domain logic. Regression now includes 16 tests: 13 domain tests and 3 UI contract tests, all passing.


---

## 17. BDD-002：家庭與孩子資料持久化 / Persistent Family and Child Data

### User / 使用者
好喔

### Assistant / 助理
開始下一個 iteration，先建立 BDD-002，不進入 SDD 或實作。

BDD-002 定義：
- 建立家庭後重新整理仍存在。
- 建立孩子後重新整理仍存在。
- 同一位家長換裝置後仍可取得自己的家庭與孩子。
- 持久化後仍必須維持 BDD-001 的家庭隔離。
- 永久儲存失敗時必須明確告知，不能讓家長誤以為已保存。

Google Sheet + Apps Script 是目前預定的實作方向，但保留到 SDD 再定義。

BDD file:
`docs/bdd/BDD-002-persistent-family-data.feature`

Commit:
`558498c6f29843498adf7a9d4c8a3bf6ce49d79b`

Commit message:
`docs(bdd): add BDD-002 persistent family data`

**English summary:**  
BDD-002 defines persistence across reloads and devices while preserving family isolation and requiring explicit failure feedback when durable storage fails. Google Sheets + Apps Script is intentionally deferred to SDD.


---

## 18. SDD-002：Google Sheet + Apps Script 持久化設計

### User / 使用者
ok

### Assistant / 助理
BDD-002 視為已確認，進入 SDD-002。

SDD-002 決策：
- Google Sheet 作為 durable storage。
- Google Apps Script 作為 server-side application / persistence layer。
- Apps Script HTML Service 作為 BDD-002 的驗收 UI，以 `google.script.run` 呼叫 server function。
- GitHub 繼續作為程式與規格的 source of truth。
- Browser 不直接取得 Sheet 編輯權限，也不直接使用 Google Sheets API credential。
- Browser localStorage 不是 source of truth。
- 資料表首版使用 `families`、`students`、`schema_meta`。
- Family / Student ID 由 server side 產生，不從顯示名稱推導。
- 所有 Sheet writes 使用 script lock 防止共享資源的並行碰撞。
- UI 不採 optimistic persistence；Sheet 確認成功後才顯示「已儲存」。
- 持久化失敗需回傳明確 error code。
- BDD-001 的家庭隔離必須持續 regression。
- 正式 Authentication 尚未完成前，只允許 Demo principals；不得輸入真實學生、家庭或零用金資料。

SDD file:
`docs/sdd/SDD-002-persistent-family-data.md`

Commit:
`1e367eebf16b42b434f24cd0bb6280f028fed21d`

Commit message:
`docs(sdd): design BDD-002 Google Sheets persistence`

**English summary:**  
SDD-002 defines Google Sheets as durable storage and Apps Script as the server-side persistence layer. The browser never accesses Sheets directly. Writes are acknowledged only after durable success, concurrent writes are guarded, family isolation remains mandatory, and the public demo must not contain real family/student data until production authentication exists.


---

## 19. README 補強 + BDD/SDD-002 確認 / README Update + BDD/SDD-002 Approval

### User / 使用者
好喔  
我們是否該在readme寫點東西  
目前BDD/SDD 002 ok

### Assistant / 助理
README 已補成專案入口文件，內容包含：

- QeKStudy 的產品目的
- Agile / BDD / SDD / TDD / Regression 流程
- 「沒有可操作 UI 就不算 Done」的 Definition of Done
- BDD-001 已完成狀態
- BDD-002 / SDD-002 已確認、下一步 TDD
- GitHub Pages Demo URL
- Demo 身份不是正式 Authentication 的安全提醒
- 本機執行方式
- npm test
- Repository structure
- 專案原則

同時把 SDD-002 狀態從 `Draft for TDD` 更新為 `Approved for TDD`。

README commit:
`c6b1fbebadfcdc1994876a6630df3a54c299550b`

SDD-002 approval commit:
`8c91baf17fa3dbb8e5b0c6bdb62e1124813c4cc4`

**English summary:**  
The README now acts as the project entry point with purpose, workflow, current story status, demo instructions, safety notes, local setup, tests, and repository structure. BDD-002 / SDD-002 are approved and ready for TDD.


---

## 20. TDD-002 Red Phase / BDD-002 持久化紅燈階段

### User / 使用者
繼續吧

### Assistant / 助理
進入 TDD-002 Red 階段，只建立測試，不寫持久化實作。

新增：
- `test/persistent-family-service.test.js`
- `test/persistence-ui-controller.test.js`
- `test/apps-script-persistence-contract.test.js`
- `docs/tdd/TDD-002-persistent-family-data.md`

BDD-002 新增 12 個測試契約，覆蓋：
- Family / Student 跨 application session 持久化
- 同一 principal 跨 session 取回相同資料
- 持久化後仍維持家庭隔離
- 直接使用其他 familyId 仍被拒絕
- write failure 不得回成功
- schema mismatch
- persistence busy
- UI 必須等 server ACK 才顯示成功
- UI failure 不得把資料加入成功清單
- Apps Script adapter 必須包含 Sheet schema 與 LockService boundary

本地 Red 驗證：
- 既有 BDD-001 regression：16 / 16 通過
- 新 BDD-002 suites：預期失敗
- 失敗原因是三個實作入口尚未存在：
  - `src/persistent-family-service.js`
  - `src/persistence-ui-controller.js`
  - `apps-script/Code.gs`

Commit:
`63ead00534810e03813870827d7eefaead3f7e60`

Commit message:
`test: add failing tests for BDD-002 persistence`

**English summary:**  
TDD-002 entered the Red phase with 12 new persistence/UI/Apps Script contract tests. All 16 existing BDD-001 regression tests remain green, while the new BDD-002 suites fail for the expected reason: the persistence service, UI controller, and Apps Script adapter do not exist yet.


---

## 21. BDD-002 Green 實作進度 / Persistence Green Implementation Progress

### User / 使用者
下一步

### Assistant / 助理
已進入 BDD-002 Green 實作，完成以下項目：

GitHub code:
- `src/persistent-family-service.js`
- `src/persistence-ui-controller.js`
- `apps-script/Code.gs`
- `apps-script/Index.html`
- `apps-script/appsscript.json`

實體 Demo DB:
- Google Sheet: `QeKStudy Database (Demo)`
- Spreadsheet ID: `16GjrllU2rRgUBXH7evGQCFqOTHQbhtJRFX6CjkvKBA8`
- tabs:
  - `families`
  - `students`
  - `schema_meta`
- `schema_version = 1`
- timezone: Asia/Taipei
- 已放入 Drive `ChatGPT` folder

持久化程式已包含：
- server-side family filtering
- server-generated family/student UUID
- Apps Script `LockService.getScriptLock()`
- schema version check
- explicit `PERSISTENCE_FAILED` / `PERSISTENCE_BUSY`
- Sheet ACK 成功後 UI 才顯示成功
- Demo Parent A / Parent B persistent UI
- 明確警告不要輸入真實資料

另外新增 GitHub Actions CI：
- `.github/workflows/test.yml`
- 每次 push / PR 自動跑 `npm test`

Commits:
- `a878ed97030b3d835be75979bd82ad3e49ec7f5d`
  - `feat: implement BDD-002 persistence green layer`
- `8158476e4757bc13026b91aee28ef9cc07f72a7f`
  - `ci: run regression on every push`

目前 BDD-002 **尚未 Done**：
1. GitHub Actions regression 仍在執行中，尚未取得 PASS 結果。
2. Apps Script source 已完成，但目前連接器沒有 Apps Script Web App deployment action，因此尚未建立可直接操作的 deployed Apps Script URL。
3. 依 Definition of Done，必須等 deployed UI 可人工驗收「建立 → reload → 仍存在 → Parent B 看不到」後才能關閉 BDD-002。

**English summary:**  
BDD-002 Green implementation is in place, including the real Google Sheet demo database, Apps Script persistence code/UI, persistence service/controller, and automatic GitHub Actions regression. The story is intentionally not marked Done yet because CI has not completed and the Apps Script Web App still needs an actual deployment for manual persistence acceptance.


---

## 22. BDD-002 Regression Passed / Manual Deployment Step Required

### User / 使用者
需要我做什麼嗎  
還是你可以直接繼續

### Assistant / 助理
能自動完成的部分已繼續完成。

Regression:
- Total tests: 28
- Passed: 28
- Failed: 0

目前只剩一個人工步驟：建立並部署 Google Apps Script Web App。

原因：目前可用連接器能建立 Google Sheet、更新 GitHub、驗證 CI，但沒有直接建立/部署 Google Apps Script Web App 的能力。

已新增部署指南：
`docs/deploy/BDD-002-apps-script.md`

部署完成後，使用者只需要把 `https://script.google.com/macros/s/.../exec` URL 貼回對話，後續可以繼續做 UI acceptance、Sheet 寫入驗證、最後 regression 與 BDD-002 Done。

Commits:
- `b8e87fa9e4b23cd2bb6af8fc12a518e5d39dc9f8`
- `3b908af1f6df08aa65eabb868a5e7066ec661524`

**English summary:**  
All automatable work is complete and regression passes 28/28. The only remaining manual step is creating/deploying the Google Apps Script Web App. A deployment guide is stored in the repository; once the deployed /exec URL is returned, final UI acceptance can continue.


---

## 23. Apps Script Deployment URL Received / UI Acceptance Pending

### User / 使用者
Provided deployed Apps Script Web App URL.

### Assistant / 助理
Deployment URL 已記錄。

Automated checks:
- Demo DB schema 正常。
- `families` 目前只有 header。
- `students` 目前只有 header。
- `schema_meta` 有 `schema_version = 1`。
- Spreadsheet timezone: Asia/Taipei。
- GitHub Actions regression previously verified 28/28 PASS.

限制：
- 標準網頁抓取工具無法直接執行 Apps Script HTML Service 的互動式 UI，因此最後的 UI acceptance 需要使用者在瀏覽器操作一次。

Repository updates:
- README 已更新為 Green / UI acceptance pending。
- Deployment doc 已記錄 deployed URL。
- TDD-002 已標記 deployment complete / UI acceptance pending。
- 新增 `docs/acceptance/Acceptance-002-persistent-family-data.md`。

Manual acceptance uses demo-only data:
- Parent A → Demo Family A → Demo Child A → reload
- Parent B must not see Parent A data
- Parent B → Demo Family B → reload
- Switch back to Parent A and verify isolation

完成 UI 操作後，Assistant 可直接讀 Google Sheet 驗證實際 durable rows，再完成 Acceptance-002 與 BDD-002 Done。

**English summary:**  
The Apps Script deployment URL has been recorded. Automated regression and database schema checks are good. Only the final interactive browser acceptance remains; after the user performs it with demo data, the assistant can verify the resulting Google Sheet rows and close BDD-002.


---

## 24. BDD-002 Done + BDD-003 Draft / Admin Data Management

### User / 使用者
確認 deployed UI 正常，並新增需求：
- 家長可以修改名稱。
- 管理員可以協助修改名稱。
- 管理員可以刪除資料。

### Assistant / 助理
BDD-002 已正式關閉：
- 使用者回報 UI acceptance 正常。
- Google Sheet 驗證到 3 個 family rows 與 5 個 student rows。
- parent-a / parent-b ownership 分離正確。
- family/student IDs 為 server-generated stable IDs。
- schema_version = 1。
- Regression 28/28 PASS。
- Acceptance-002 / TDD-002 / README 已更新為 Done。

新增 BDD-003：
`docs/bdd/BDD-003-admin-data-management.feature`

BDD-003 Draft 規則：
- 家長可修改自己家庭的顯示名稱。
- 家長可修改自己家庭中孩子的顯示名稱。
- 改名不得改變 familyId / studentId / family ownership。
- 家長不可修改其他家庭資料。
- 一般家長沒有刪除權限。
- 管理員可跨家庭協助修改家庭與孩子名稱。
- 管理員可刪除孩子。
- 管理員可刪除家庭；刪除後不得留下仍可被一般使用者存取的 orphan child data。
- 非管理員即使知道 familyId / studentId 也不能呼叫 admin operation。
- destructive delete UI 必須有明確確認。
- hard delete vs soft delete / recovery 留給 SDD 決定。
- 正式 Authentication / Admin identity 尚未完成前只使用 Demo data。

Commits:
- README BDD-002 Done / BDD-003 status: `312d9979aecbd36de40c7197b79ea16369acdc84`
- Acceptance-002 Done: `f66a976e62eef03d3c4e65f4d487399eb3669708`
- TDD-002 closed: `0344e161f965ae044e94380836c72f258706dfdf`
- BDD-003 draft: `11d6121b54d77894b849d22ff8c2e42acca1d13d`

**English summary:**  
BDD-002 is complete after successful deployed UI acceptance, durable Sheet verification, and 28/28 regression. BDD-003 now defines parent rename rights, administrator cross-family rename support, administrator-only deletion, stable IDs during renames, protection against cross-family access, confirmation for destructive deletion, and no accessible orphan child records after family deletion.


---

## 25. BDD-003 Approved / SDD-003 Draft

### User / 使用者
好

### Assistant / 助理
BDD-003 視為已確認，進入 SDD-003。

主要設計決策：
- 家長可改自己家庭與孩子名稱。
- 管理員可跨家庭協助改名。
- 一般家長沒有刪除權限。
- 管理員刪除採 **soft delete**，不直接刪 Google Sheet row。
- 改名不改 stable IDs 或 family ownership。
- family delete 需一併 soft-delete 該家庭所有 active students，避免 accessible orphan data。
- 新增 append-only `audit_log`，記錄 rename/delete。
- schema version 從 1 升到 2。
- `families` / `students` 新增 deletion metadata。
- public demo 在正式 Authentication/Admin identity 前不得暴露可被 browser 冒用的 admin destructive endpoint。
- parent rename 可以在 demo 模式驗收；admin destructive behavior 先在安全 test boundary 驗證。
- full regression 必須維持 BDD-001 + BDD-002 全綠。

SDD:
`docs/sdd/SDD-003-admin-data-management.md`

Commit:
`88fed3b40d488d5422f823ab6390e838721505a9`

README status:
BDD Approved / SDD Draft

README commit:
`193c6c4cf9a948dfaab02f2b6111a27069612f81`

**English summary:**  
BDD-003 is approved. SDD-003 proposes soft deletion, stable IDs during renames, administrator-only destructive operations, append-only audit logging, schema migration from v1 to v2, cascading soft deletion of students when a family is deleted, and a strict rule not to expose public administrator deletion until real authentication/admin identity exists.
